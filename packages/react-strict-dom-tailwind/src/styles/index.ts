import { tailwindStyles } from './tailwind.styles';
import { customStyles } from './customStyles';

export const styles = {
    ...tailwindStyles,
    ...customStyles
}

export {
    tailwindStyles,
    customStyles
}