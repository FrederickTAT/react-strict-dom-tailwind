import { tailwindStyles } from './tailwind.styles';
import { dynamicStyles } from './dynamic.styles';

export const styles = {
    ...tailwindStyles,
    ...dynamicStyles
}

export {
    tailwindStyles,
    dynamicStyles
}